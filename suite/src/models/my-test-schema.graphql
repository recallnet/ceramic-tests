type GenericModel @createModel(accountRelation: LIST, description: "Very basic profile") {
    numericalField: Int @int(min: 1, max: 100)
    textField: String @string(minLength: 5, maxLength: 50)
    booleanField: Boolean
}